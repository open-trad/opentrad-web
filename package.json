{
  "name": "opentrad-web",
  "version": "1.0.0",
  "private": true,
  "description": "OpenTrad 开源商贸单证工具包",
  "license": "AGPL-3.0-only",
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@10.28.2",
  "scripts": {
    "build": "pnpm -r --if-present build",
    "dev": "pnpm --filter @opentrad/document-core build && pnpm --filter @opentrad/web dev",
    "e2e": "pnpm --filter @opentrad/web exec playwright test",
    "golds:generate": "node scripts/generate-template-golds.mjs",
    "golds:verify": "node --test scripts/gold-verification-helpers.test.mjs && node scripts/verify-template-golds.mjs",
    "lint": "biome check .",
    "test": "pnpm --filter @opentrad/document-core build && pnpm --workspace-concurrency=1 -r --if-present test",
    "typecheck": "pnpm --filter @opentrad/document-core build && pnpm -r --if-present typecheck"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.2",
    "typescript": "5.9.2"
  }
}
